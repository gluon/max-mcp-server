{
  "mcpServers": {
    "maxmsp": {
      "command": "uv",
      "args": ["--directory", "${PLUGIN_DIR}", "run", "python", "-m", "maxmsp_mcp.server"],
      "env": { "MAX_HOST": "127.0.0.1", "MAX_PORT": "7400" }
    }
  }
}
